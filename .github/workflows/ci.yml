name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run artifact check
        run: .\scripts\check_artifact.cmd
        shell: cmd
